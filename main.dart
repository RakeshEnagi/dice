import 'package:flutter/material.dart';
import 'package:quiz/Quiz.dart';
// to run the app on mobile first the mobile and pc should be connect with same wifi
//then use adb pair and adb connect
void main() {
  runApp(const Quiz());
}
